                    <div class="panel-body">
                        <%= form_for(@news) do |f| %>
                        <% if @news.errors.any? %>
                        <div id="error_explanation">
                          <h2><%= pluralize(@news.errors.count, "error") %> prohibited this news from being saved:</h2>

                          <ul>
                          <% @news.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                          <% end %>
                          </ul>
                        </div>
                      <% end %>
                            <div class="form-group">
                            <%= f.text_field :title, :placeholder => 'Title', :class => 'form-control input-sm', :id => 'title' %>
                            </div>

                            

                            <div class="row">
                                <div class="col-xs-6 col-sm-6 col-md-6">
                                    <div class="form-group">
                                    <%= f.text_field :content, :placeholder => 'Content', :class => 'form-control input-sm', :id => 'content' %>
                                    </div>
                                </div>
                                <div class="col-xs-6 col-sm-6 col-md-6">
                                    <div class="form-group">
                                        <%= f.text_field :img_url, :placeholder => 'Img Url', :class => 'form-control input-sm', :id => 'img_url' %>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                 <div class="col-xs-6 col-sm-6 col-md-6">
                                    <div class="form-group">
                                <%= f.text_field :votes, :placeholder => 'Votes', :class => 'form-control input-sm', :id => 'votes' %>
                                    </div>
                                </div>
                            </div>
                            <%= submit_tag("Submit", :class => "btn btn-info btn-block") %>
                            <% end %>
                    </div>
                </div>
            </div>
        </div>
    