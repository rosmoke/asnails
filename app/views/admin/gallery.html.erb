<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Asnails</h1>
    <ol class="breadcrumb">
      <li class="active"><i class="fa fa-dashboard"></i> Gallery settings</li>
    </ol>
  </div>
</div>
<div class="row">
  <div class="col-lg-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><i class="fa fa-long-arrow-right fa-fw"></i> Upload images for header</h3>
      </div>
      <div class="panel-body"><h5>Please keep in mind that preffered resolution is 1041px * 330px.</h4>
        <div id="morris-donut-chart">
          <%= form_for @header do |f| %>
          <div class="form-group">
            <%= f.label :title %>
            <%= f.text_field :title, class: 'form-control textfieldd', required: true %>
          </div>
          <div class="form-group">
            <%= f.label :image %>
            <%= f.file_field :image, required: true %>
          </div>
          <%= f.submit 'Upload', class: 'btn btn-primary btn-lg' %>
          <% end %>
          <ul class="row" id="photos-list">
          <% @headers.each do |header| %>
          
            <li class="col-xs-3">
              <%= link_to image_tag(header.image.thumb('180x180#').url, alt: header.title, class: 'img-thumbnail'),
                          header.image.url, target: '_blank' %>
                          
              <p><%= header.title %></p>
              <%= button_to 'Delete', header_path(header), :method => :delete, confirm: 'Are you sure?' %>
            </li>
          <% end %>
        </ul>
    </div>
</div>
</div>
</div>
<div class="col-lg-4">
<div class="panel panel-default">
<div class="panel-heading">
    <h3 class="panel-title"><i class="fa fa-clock-o fa-fw"></i> Upload images for gallery</h3>
</div>
<div class="panel-body"><h5>Please keep in mind that preffered resolution is 800px * 360px.</h4>
    <div class="list-group"> 
        <%= form_for @photo do |f| %>
          <div class="form-group">
            <%= f.label :title %>
            <%= f.text_field :title, class: 'form-control textfieldd', required: true %>
          </div>
         
          <div class="form-group">
            <%= f.label :image %>
            <%= f.file_field :image, required: true %>
          </div>
         
          <%= f.submit 'Upload', class: 'btn btn-primary btn-lg' %>
        <% end %>
         
        <ul class="row" id="photos-list">
        
          <% @photos.each do |photo| %>
            <li class="col-xs-3">
              <%= link_to image_tag(photo.image.thumb('180x180#').url, alt: photo.title, class: 'img-thumbnail'),
                          photo.image.url, target: '_blank' %>
                          
              <p><%= photo.title %></p>
              <%= button_to 'Delete', photo_path(photo), :method => :delete, confirm: 'Are you sure?' %>
            </li>
          <% end %>
        </ul></p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-lg-4">
                                <div class="panel panel-default">
                                    <div class="panel-heading">
                                        <h3 class="panel-title"><i class="fa fa-money fa-fw"></i> Something Else</h3>
                                    </div>
                                    <div class="panel-body">
                                        <div class="table-responsive">
                                           
                                        </div>
                                        <div class="text-right">
                                            <a href="#">View All Transactions <i class="fa fa-arrow-circle-right"></i></a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>