 <script type="text/javascript">
$(document).ready(function() {
    
    $('.simple-ajax-popup').magnificPopup({
        type: 'ajax'
    });
    
});

    </script>
  <div class="col-lg-12">
    <h1 class="page-header">Asnails</h1>
    <ol class="breadcrumb">
      <li class="active"><i class="fa fa-dashboard"></i> News settings</li>
    </ol>
  </div>



  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><i class="fa fa-long-arrow-right fa-fw"></i> Manage articles</h3>
      </div>
      <div class="panel-body"> <button id="demo15" class="btn btn-success">New Article</button>
      <%= render 'news/new' %>
           <hr></hr>
        <div class="table-responsive">
          <table class="table table-striped table-bordered table-hover" id="dataTables-example">
              <thead>
                  <tr>
                      <th>Title</th>
                      <th>Content</th>
                      <th>Image</th>
                      <th>Created on</cth>
                      <th>Votes</th>
                  </tr>
              </thead>
              <tbody>
                
              <% @newsall.each do |news|  %>
                  <tr class="gradeU">
                      <td><%= link_to news.title, edit_news_path(news), :class => "simple-ajax-popup" %>

                      </td>
                      <td><%= news.content[0...60] %></td>
                      <td><%= news.img_url %></td>
                      <td class="center"><%= news.created_at.strftime("%m/%d/%Y - %H:%M") %></td>
                      <td class="center"><%= button_to 'Delete', news, :class => "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
                  </tr>
                  <% end  %>
              </tbody>
          </table>
      </div>
    </div>
  </div>
</div>